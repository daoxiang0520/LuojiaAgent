services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    volumes:
      # 持久化会话数据（SqliteSaver 用）
      - ./data:/app/data
    restart: unless-stopped
